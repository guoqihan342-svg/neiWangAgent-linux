# neiWangAgent v0.2.0 操作手册

> 本地 MCP Agent — 自动改代码 → commit → push → 创建 MR
> 更新日期：2026-05-11  |  版本：v0.2.0

---

## 1. 版本说明

| 版本 | 仓库 | 适用系统 |
|------|------|---------|
| **Linux版** | `guoqihan342-svg/neiWangAgent-linux` | Linux/Mac/WSL |
| **Windows版** | `guoqihan342-svg/neiWangAgent` | Windows/WSL |

> ⚠️ 两个版本代码保持同步，修改需双仓库一起推送。

---

## 2. 环境要求

| 项目 | 要求 |
|------|------|
| Python | 3.10+ |
| Git | 已配置 user.name / user.email |
| 网络 | 能访问 LLM API（内网或外网） |
| 磁盘 | 项目 + `.agent/` 目录（约 50MB） |

---

## 3. 安装

```bash
cd neiWangAgent
pip install -e .
neiWangAgent --version  # 验证安装
```

---

## 4. 配置

### 4.1 环境变量（必须）

```bash
# LLM 配置 — 全部走环境变量，不硬编码
export LLM_BASE_URL="http://your-llm-server:8080/v1"
export LLM_MODEL="your-model-name"
export LLM_API_KEY="your-api-key"
```

### 4.2 初始化项目

```bash
# 内网默认（推荐）
neiWangAgent init

# GitHub 自测（需额外设置 GITHUB_TOKEN）
neiWangAgent init --profile github-demo
export GITHUB_TOKEN="ghp_xxx"
```

### 4.3 config.yaml 关键配置

```yaml
project:
  project_type: "java"        # java/python/go/typescript/generic

runtime:
  llm_base_url: "${LLM_BASE_URL}"  # 从环境变量读取

mr:
  provider: "internal_mcp"    # internal_mcp / github / mock

task:
  enable_self_review: false   # 启用提交前 LLM 自审
  budget_cents: 20            # LLM 费用上限（分）
```

---

## 5. 日常使用

### 5.1 构建知识库

```bash
neiWangAgent warmup
# 输出: [summary] → [hotspot] → [deep]
# 索引结果持久化到 .agent/knowledge/
```

### 5.2 执行任务

```bash
# 方式1：命令行
neiWangAgent run --task "为 UserController 添加分页查询接口"

# 方式2：任务文件
echo "为 UserController 添加分页查询接口" > task.md
neiWangAgent run --task task.md

# dry-run 模式（不写文件不推送）
neiWangAgent run --task task.md --dry-run
```

### 5.3 恢复中断

```bash
# 查看运行记录
ls .agent/runs/

# 恢复（自动加载澄清答案）
neiWangAgent resume 20260511-143022
```

### 5.4 查看报告

```bash
cat .agent/runs/20260511-143022/report.md
```

---

## 6. 状态机流程（18步）

```
INIT → WARMUP_CHECK → SUMMARY_REFRESH → WORKTREE_GUARD →
LOAD_REQUIREMENT → RETRIEVE_CONTEXT → UNDERSTAND_REQUIREMENT →
CLARIFICATION_GATE → PLAN_IMPLEMENTATION → CREATE_BRANCH →
IMPLEMENT → SELF_REVIEW → CHANGE_SCOPE_GUARD →
DATABASE_IMPACT_DETECT → PREPARE_COMMIT → COMMIT →
PUSH → CREATE_MR → DONE
```

**状态分级**：
- CRITICAL（8个）：失败→停止，不允许跳过
- OPTIONAL（4个）：失败→降级跳过
- HUMAN（2个）：失败→暂停等待

---

## 7. MCP Server 说明

| Server | 功能 | 配置 |
|--------|------|------|
| Knowledge | 三层索引 + MyBatis/Vue深度 + 持久化搜索 | `knowledge.layers` |
| Git | branch/commit/push（安全校验） | `git.*` |
| MR | Provider模式创建MR | `mr.provider` |
| Database | DDL解析 + 只读验证 + 影响检测 | `database.*` |
| Clarification | 澄清问答（文件存档） | `clarification.*` |
| Requirement | 需求读取/解析 | — |

---

## 8. 安全红线

| 操作 | 状态 |
|------|------|
| git commit（显式 files，禁止 add -A） | ✅ |
| git push agent/*（禁止 force push） | ✅ |
| MR 创建（Provider 隔离） | ✅ |
| push master/main/release/hotfix | ❌ |
| git merge / force push | ❌ |
| INSERT/UPDATE/DELETE/DROP/TRUNCATE | ❌ |
| sudo / rm -rf / chmod 777 / fork bomb | ❌ |

---

## 9. 日志系统

```
.agent/logs/agent.log          # JSON Lines 结构化日志
.agent/runs/{id}/report.md     # ★ 每次运行运维报告
.agent/runs/{id}/state.json    # 状态持久化
.agent/runs/{id}/mr_description.md  # MR 描述
.agent/runs/{id}/clarification/     # 澄清存档
```

---

## 10. 目录结构

```
├── config.yaml
├── pyproject.toml
├── Makefile
├── src/agent_mcp/
│   ├── orchestrator.py      # ★ 18步状态机(100% MCP)
│   ├── llm_client.py        # ★ 预算控制
│   ├── code_parser.py       # ★ 7格式 + apply_unified_diff
│   ├── config_loader.py     # ★ profiles + env解析
│   ├── knowledge_server.py  # ★ MyBatis/Vue深度索引
│   ├── database_server.py   # ★ DDL解析 + 只读验证
│   ├── git_server.py        # ★ 安全校验
│   ├── mr_server.py         # ★ Provider模式
│   ├── clarification_server.py  # ★ 文件存档
│   ├── requirement_server.py
│   ├── base_mcp.py          # MCP基类
│   ├── mcp_client.py        # stdio客户端
│   ├── tracing.py           # 结构化日志
│   └── cli.py               # CLI入口
├── tests/                   # ★ 86项测试
├── .agent/
│   ├── logs/
│   ├── knowledge/
│   └── runs/
└── business-docs/
```

---

## 11. 常见问题

**Q: 报 `LLM_API_KEY is not set`**
```bash
export LLM_API_KEY="your-key"
```

**Q: 内网如何使用？**
```bash
# 设置内网 LLM 地址
export LLM_BASE_URL="http://internal-llm:8080/v1"
export LLM_MODEL="internal-model"
export LLM_API_KEY="internal-key"
neiWangAgent init  # 默认 internal profile
```

**Q: 如何查看执行日志？**
```bash
cat .agent/logs/agent.log | jq .
cat .agent/runs/20260511-143022/report.md
```

**Q: 预算耗尽怎么办？**
```yaml
# config.yaml
task:
  budget_cents: 100  # 调大预算
```
