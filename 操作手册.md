# neiWangAgent v0.1 操作手册

> 本地无服务器 MCP Agent — 自动改代码 → commit → push → 创建 MR

---

## 1. 环境要求

| 项目 | 要求 |
|------|------|
| Python | 3.10+ |
| Git | 已配置 user.name / user.email |
| 网络 | 能访问 DeepSeek API 和 GitHub |
| 磁盘 | 代码项目 + `.agent/` 目录（约 50MB） |

---

## 2. 安装

```bash
# 进入项目目录
cd F:\agent\neiWangAgent\code

# 安装依赖（WSL 需要 --break-system-packages）
pip install --break-system-packages -e .

# 普通 Linux/Mac
pip install -e .
```

验证安装：
```bash
agent --help
# 输出: Usage: agent [OPTIONS] COMMAND [ARGS]...
#   Commands: init, warmup, run, resume
```

---

## 3. 配置

### 3.1 设置 API Key

创建 `.env` 文件（或直接设环境变量）：

```bash
# Linux/WSL
export DEEPSEEK_API_KEY="sk-你的key"

# Windows PowerShell
$env:DEEPSEEK_API_KEY="sk-你的key"
```

### 3.2 设置 GitHub Token（用于创建 MR）

```bash
export GITHUB_TOKEN="ghp_你的token"
```

### 3.3 修改 config.yaml

打开 `config.yaml`，按需要修改：

```yaml
# 关键配置项
project:
  name: "你的项目名"
  default_branch: "master"     # 目标分支

runtime:
  llm_base_url: "https://api.deepseek.com/v1"
  llm_model: "deepseek-v4-flash"  # 或 deepseek-v4-pro

git:
  target_branch: "master"
  branch_prefix: "agent/"       # 分支名前缀
```

---

## 4. 四个命令

### 4.1 `agent init` — 初始化

```bash
agent init
```

做的事：
- 创建 `.agent/` 目录结构
- 创建 `.agent/templates/mr_description.md`
- 创建 `.agent/runs/` 运行记录目录
- 创建 `business-docs/` 业务文档目录
- 确认 `config.yaml` 存在

### 4.2 `agent warmup` — 预热知识库

```bash
agent warmup
```

做的事：
- **Summary 层**：扫描代码文件统计（.py/.java/.vue等）
- **Hotspot 层**：分析核心模块（v0.1 预留）
- **Deep 层**：深度索引（v0.1 预留）

输出：
```
🚀 开始预热知识库...
[summary] 代码库扫描: 126 个 .py 文件, 45 个 .vue 文件
[hotspot] v0.1 简化版，跳过深度分析
✅ 预热完成
```

### 4.3 `agent run` — 执行任务（核心命令）

```bash
# 直接传入需求文本
agent run --task "给 User 表加一个 phone 字段"

# 从文件读取需求
agent run --task requirement.md

# 用 Pro 模型深度推理
agent run --task requirement.md --model deepseek-v4-pro
```

**完整流程**（23 步状态机）：

```
🚀 Agent 运行 [20260510-223000]
📋 给 User 表加一个 phone 字段

  [000] 初始化
  [010] 检查知识库
  [015] 刷新摘要
  [018] 工作区保护检查
  [020] 加载需求
  [030] 检索上下文
  [040] 理解需求
  [050] 澄清判断
  [100] 生成实施计划
  [110] 创建分支        → agent/20260510-auto
  [120] 修改代码        → src/models/User.java
  [125] 变更范围检查
  [130] 数据库影响检测
  [150] 准备提交
  [160] 执行提交        → feat: 给 User 表加 phone 字段
  [170] 推送代码        → origin/agent/20260510-auto
  [180] 创建 MR         → https://github.com/.../mr/...
  [200] 完成
```

### 4.4 `agent resume` — 恢复中断

```bash
agent resume 20260510-223000
```

恢复后从上次中断的状态继续执行。

---

## 5. 目录结构

```
code/
├── config.yaml              ← 配置文件（改这里）
├── pyproject.toml           ← pip install 用的
├── src/agent_mcp/
│   ├── cli.py               ← CLI 入口
│   ├── orchestrator.py      ← 状态机编排器（核心）
│   ├── llm_client.py        ← LLM 客户端
│   ├── mcp_client.py        ← MCP stdio 客户端
│   ├── config_loader.py     ← 配置加载器
│   ├── knowledge_server.py  ← Knowledge MCP
│   ├── requirement_server.py← Requirement MCP
│   ├── git_server.py        ← Git MCP
│   ├── mr_server.py         ← MR MCP
│   ├── clarification_server.py ← Clarification MCP
│   └── database_server.py   ← Database MCP
├── business-docs/           ← 放业务文档/DDL
├── .agent/
│   ├── templates/mr_description.md
│   └── runs/                ← 每次运行的记录
└── .gitignore
```

---

## 6. 安全红线

| 操作 | 允许 |
|------|------|
| 读写工作目录 | ✅ |
| git push agent/* | ✅ |
| 创建 MR | ✅ |
| 读 DDL 文件 | ✅ |
| push master/main | ❌ |
| git merge | ❌ |
| 执行 SQL (INSERT/UPDATE/DELETE) | ❌ |
| 操作 .env / .ssh / *.pem | ❌ |

---

## 7. 常见问题

**Q: 报 `ModuleNotFoundError: No module named 'agent_mcp'`**
```bash
# 设置 PYTHONPATH
export PYTHONPATH=/mnt/f/agent/neiWangAgent/code/src:$PYTHONPATH
```

**Q: 报 `DEEPSEEK_API_KEY is not set`**
```bash
export DEEPSEEK_API_KEY="sk-你的key"
```

**Q: 报 `GITHUB_TOKEN is not set`**
```bash
export GITHUB_TOKEN="ghp_你的token"
```

**Q: 运行后没有实际修改代码？**
v0.1 中 LLM 通过 `@@FILE:path@@` 标记输出代码修改。确保需求足够具体。

---

## 8. 快速上手示例

```bash
# 1. 安装
cd F:\agent\neiWangAgent\code
pip install --break-system-packages -e .

# 2. 配置
export DEEPSEEK_API_KEY="sk-xxx"
export GITHUB_TOKEN="ghp_xxx"

# 3. 初始化
agent init

# 4. 预热
agent warmup

# 5. 跑一个任务
agent run --task "在 README.md 末尾添加一行版本号 v0.1.0"

# 6. 查看结果
ls .agent/runs/
cat .agent/runs/20260510-*/state.json
```

---

> 📖 开发方案详见 `F:\agent\neiWangAgent\本地MCP_Agent_开发方案_v4_Agent版.md`
> 🐛 问题反馈：https://github.com/guoqihan342-svg/neiWangAgent/issues
